# Um cluster = um bloco [k3s_server] (exatamente 1 host) + [k3s_agents] (0+ hosts).
# Pra outro cluster/ambiente, duplique este arquivo (ex: hosts-staging.ini) com seus próprios grupos.

[k3s_server]
vps-1 ansible_host=187.127.62.20 tailscale_hostname=vps-1

[k3s_agents]
# worker-1 ansible_host=1.2.3.4 tailscale_hostname=worker-1

[k3s_cluster:children]
k3s_server
k3s_agents

[k3s_cluster:vars]
ansible_user=deploy
