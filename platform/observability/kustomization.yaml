# Manifests extras do stack. O chart kube-prometheus-stack vem como source Helm no Application monitoring.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
  - manifests/tailnet-only-middleware.yaml
  - manifests/alertmanager-discord.yaml
  - manifests/cert-manager-podmonitor.yaml
